import { NotNumberDirective } from './not-number.directive';

describe('NotNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NotNumberDirective();
    expect(directive).toBeTruthy();
  });
});
